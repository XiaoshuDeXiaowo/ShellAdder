name: Upload Python Package to PyPI

on:
  push:
    tags:
      - 'v*'               # 当推送以 "v" 开头的标签时触发（如 v1.0.0）
  # 也可以根据需要手动触发或基于其他分支
  workflow_dispatch:        # 允许手动触发

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # 允许生成 OIDC 令牌
      contents: read    # 检出代码所需（通常必须）
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.8'  # 使用最新的 Python 3

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build          # 安装构建工具

    - name: Build package
      run: python -m build         # 生成 dist/ 下的 .tar.gz 和 .whl

    - name: Publish to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        password: ${{ secrets.PYPI_API_TOKEN }}  # 使用存储在 Secrets 中的 API token
